name: fauto

services:
  bun:
    container_name: fauto-bot
    build:
      context: .
      dockerfile: .docker/Dockerfile
    command: bun dev
    volumes:
      - .:/app
      - /app/node_modules
